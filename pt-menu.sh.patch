--- pt-menu.sh
+++ pt-menu.sh
@@ -737,7 +737,6 @@
     "Set Discord Webhook"
     "Set Teams Webhook"
     "Toggle Teams Notify"
-    "Test Telegram Webhook"
-    "Test Discord Webhook"
-    "Test Teams Webhook"
+    "Test Webhooks (Send Sample)"
     "← Back"
   )
   local sel; sel=$(pick_fzf "Webhook>" "${choices[@]}")
@@ -765,24 +764,12 @@
       set_env_val "NOTIFY_TEAMS" "$next"
       echo -e "  ${GRN}✓ NOTIFY_TEAMS=${next}${RST}"
       ;;
-    "Test Telegram Webhook")
-      local wh; wh=$(env_val TELEGRAM_WEBHOOK "")
-      [[ -z "$wh" ]] && { echo -e "  ${RED}TELEGRAM_WEBHOOK not set${RST}"; read -r -p $'\nPress Enter...'; return; }
-      print_run_header "Telegram Webhook Test" "Telegram"
-      set +e
-      node "$PROJECT_DIR/docker-local-pt/scripts/webhook-tester.mjs" telegram "$wh" 2>&1
-      print_run_footer "$?"
-      ;;
-    "Test Discord Webhook")
-      local wh; wh=$(env_val DISCORD_WEBHOOK "")
-      [[ -z "$wh" ]] && { echo -e "  ${RED}DISCORD_WEBHOOK not set${RST}"; read -r -p $'\nPress Enter...'; return; }
-      print_run_header "Discord Webhook Test" "Discord"
-      set +e
-      node "$PROJECT_DIR/docker-local-pt/scripts/webhook-tester.mjs" discord "$wh" 2>&1
-      print_run_footer "$?"
-      ;;
-    "Test Teams Webhook")
-      local wh; wh=$(env_val TEAMS_WEBHOOK "")
-      [[ -z "$wh" ]] && { echo -e "  ${RED}TEAMS_WEBHOOK not set${RST}"; read -r -p $'\nPress Enter...'; return; }
-      print_run_header "Teams Webhook Test" "Teams"
+    "Test Webhooks (Send Sample)")
+      local wh;
+      wh=$(env_val TELEGRAM_WEBHOOK "")
+      [[ -n "$wh" ]] && { print_run_header "Telegram Webhook Test" "Telegram"; set +e; node "$PROJECT_DIR/docker-local-pt/scripts/webhook-tester.mjs" telegram "$wh" 2>&1; print_run_footer "$?"; }
+      wh=$(env_val DISCORD_WEBHOOK "")
+      [[ -n "$wh" ]] && { print_run_header "Discord Webhook Test" "Discord"; set +e; node "$PROJECT_DIR/docker-local-pt/scripts/webhook-tester.mjs" discord "$wh" 2>&1; print_run_footer "$?"; }
+      wh=$(env_val TEAMS_WEBHOOK "")
+      [[ -n "$wh" ]] && { print_run_header "Teams Webhook Test" "Teams"; set +e; node "$PROJECT_DIR/docker-local-pt/scripts/webhook-tester.mjs" teams "$wh" 2>&1; print_run_footer "$?"; }
       set +e
-      node "$PROJECT_DIR/docker-local-pt/scripts/webhook-tester.mjs" teams "$wh" 2>&1
-      print_run_footer "$?"
       ;;
   esac
